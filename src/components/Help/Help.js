import { useState } from 'react';

const Help = ({db}) => {
   
    const history = useHistory();
    
    return (
        <div className="help">
            <h2>Ask help from people if you are in the disaster affected area</h2>
            

        </div>
    );
}

export default Help;